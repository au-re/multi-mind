{
  "name": "@multi-mind/engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "bunx tsc -b",
    "lint": "bun run typecheck && bunx biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "@multi-mind/shared": "workspace:*",
    "@multi-mind/traits": "workspace:*",
    "@multi-mind/blackboard": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vitest": "^4.0.15"
  }
}
