{
  "name": "e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "up": "docker compose -f ../../deploy/docker-compose.yml up -d --build --wait",
    "down": "docker compose -f ../../deploy/docker-compose.yml down"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
